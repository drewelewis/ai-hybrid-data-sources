# Copy to /etc/ipsec.secrets on the OpenWrt router (chmod 600).
# Use the SAME pre-shared key you entered for the azd `sharedKey` prompt.
# The real /etc/ipsec.secrets is git-ignored — never commit the PSK.

: PSK "REPLACE_WITH_SHARED_KEY"
